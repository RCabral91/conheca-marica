import styled from 'styled-components';

export const AllPubsNRestsCards = styled.div`
  a {
    color: var(--primary);
    text-decoration: none;
    background-color: transparent;
  }
`;

export const Cover = styled.div`
  width: 100%;
  height: 250px;
  background-size: cover;
`;
